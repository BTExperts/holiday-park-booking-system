import React from 'react';
import App from './App.jsx';

// Main application router
// Currently a single-page application, but structured for future routing needs
const AppRouter = () => {
  return <App />;
};

export default AppRouter;
